services:
  zeiterfassung:
    build: .
    image: zeiterfassung:latest
    ports:
      - "7676:3000"
    volumes:
      - zeiterfassung_data:/data
    environment:
      - TZ=Europe/Berlin
    restart: unless-stopped

volumes:
  zeiterfassung_data:
